/** Define a class to merge 2 sorted integer in ascending order with the foellowing intance variable and method.
 * (a). an array to store the element of array
 * methods->void accept() to accept the element of the array in ascending order without any duplicate.
 * Mixer mix(Mixer a) to merge the current object array element with the parameterized array element and return thr resultant object.
 * void display() to display the element of the array.
 * Define the main method to test the class.
 */

import java.util.Scanner;

public class Mixer_Merge {
    
}
